name: "Test"
on:
    [push, pull_request]
jobs:
  build:
    name: run_tests_all
    runs-on: ubuntu-latest        
    steps:
    - uses: actions/checkout@v7.0.1
    - name: Set up Java
      uses: actions/setup-java@v6
      with:
        java-version: '21'
        distribution: 'adopt'
    - name: Test with Maven
      run: mvn clean test
